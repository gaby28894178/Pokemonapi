import { createSlice } from "@reduxjs/toolkit";

export const sliceSlice = createSlice({
    name: 'slice',
    initialState: null,
    reducers: {
        
    }
});

export const {  createSlice } = sliceSlice.actions;

export default sliceSlice.reducer;